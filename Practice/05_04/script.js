/**
 * Note: This file is intentionally empty.
 * You can use it to test your skills at traversing the DOM using JavaScript.
 */

let lidStatus = document.querySelector(".backpack__lid span").innerHTML.trim();
console.log(lidStatus);

let openButton = document.querySelector(".lid-toggle");
console.log(openButton);

// let openBackpack = () => {
//   if (openButton.innerHTML === "closed") {
//     lidStatus.innerHTML = "open";
//   }
// };
